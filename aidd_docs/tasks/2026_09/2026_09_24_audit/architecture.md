# Codebase Audit: plugins/design — architecture

La carte de l'entonnoir montrée par `detail` contredit `define` et `destructure`. Du WordPress subsiste dans le cœur de mesure. Des schémas sont recopiés, et des outils transverses sont rangés dans une skill.

- **Date**: 2026_09_24
- **Scope**: `plugins/design/`
- **Health**: fair
- **Findings**: 1 critical, 10 warning, 1 minor

## Findings

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | -------- | -------- | ----- | ------------- | ------ |
| 🔴 | architecture | `plugins/design/skills/detail/references/funnel-map.md:10-11` | La carte « source unique » restituée par `detail` affirme deux choses fausses. Selon elle, `define` écrit `components.json` et `policies.json` en brouillon, alors que `define/actions/04-write-material.md:16` l'interdit (« n'écrire **aucun** `design/components.json`, `policies.json`… »). Elle dit aussi que `destructure` « décompose un rendu » et écrit `components.json`, alors que c'est une critique en lecture seule (`skills/destructure/SKILL.md:3`). L'utilisateur reçoit donc une carte fausse | Réécrire les deux lignes : define → `tokens.json` + `design-system.md` ; destructure → critique, rapport, contrat inchangé. Ajouter une garde `design-behave` sur ces deux lignes | S |
| 🟡 | architecture | `plugins/design/adapters/measure/config-gen.py:451-453` | Du WordPress reste dans le cœur agnostique : `wp-admin/site-editor.php`, `WP_EDITOR_STORAGE_STATE`, `WP_EDITOR_AUTH_HOOK` (et `:500`) ; détection de `wp-login.php` (`measure.py:652-656`) ; test FSE du cœur (`tests/test_cascade_ownership.py:1`). Cela contredit la règle « une contrainte de plateforme appartient au réceptacle » (`aidd_docs/memory/design-plugin.md:88`) | Surface `ownership` générique : URL de l'éditeur, variables d'environnement et motif de login passés par la config. Profil FSE fourni par `sc-php:design-bridge`. Test FSE déplacé côté `sc-php` | L |
| 🟡 | architecture | `plugins/design/references/contract-schema.md:152-166` | Le schéma de `components.json` est recopié 3 fois, et la copie de `contract-schema.md` a déjà divergé : `states` y manque, alors qu'il est présent dans `skills/adjust/references/manifest-schema.md:21` et `02-freeze.md:62`, et que `tools/status.py:88` le lit | Supprimer le bloc JSON au profit d'un renvoi à `manifest-schema.md` (la prose de la ligne 168 le fait déjà) | S |
| 🟡 | architecture | `plugins/design/references/contract-schema.md:225-256` | Le bloc `oracle.json` (`components` + `pages`) est identique aujourd'hui à `skills/adjust/actions/02-freeze.md:83-114`, mais rien ne garde leur synchronisation : `design-behave.mjs:86-87` teste seulement la présence de chaînes | Un seul exemplaire et un renvoi, ou une comparaison octet pour octet du bloc extrait (modèle `design-behave.mjs:143-153`) | S |
| 🟡 | architecture | `plugins/design/skills/adjust/references/manifest-schema.md:1` | Un schéma partagé vit dans les internes d'une skill. Il est cité par `define/actions/02-extract.md:29`, `define/actions/03-construct.md:28`, `destructure/actions/01-challenge.md:30`, `references/token-schema.md:386`, `references/write-system-procedure.md:19` et `docs/concepts.md:57` | Le déplacer dans `plugins/design/references/` et mettre à jour les 6 renvois | M |
| 🟡 | architecture | `plugins/design/skills/enforce/adapters/lint-core.mjs:1` | Un outil transverse est rangé dans les internes d'`enforce`. Il sert d'oracle de scan au gel, de gate à diffuse et de runner. Il est consommé par `adjust/actions/02-freeze.md:268`, `adjust/actions/03-migrate.md:24`, `diffuse/actions/02-render.md:10`, `tools/run-gates.py:5` et `references/enforcement-registry.md:11` | Le déplacer dans `tools/`, à côté de `run-gates.py` et `status.py`, avec qui il forme le trio installé. Adapter `design-behave.mjs:146` | M |
| 🟡 | architecture | `plugins/design/skills/define/actions/03-construct.md:46` | Des actions de skills différentes se citent mutuellement : `define/03-construct` ↔ `enforce/05-fidelity-gate.md:148,170`. D'autres fichiers pointent aussi vers une section interne d'`enforce` : `agents/copycat.md:38`, `references/gate-natures.md:33`, `references/visual-diff-procedure.md:7`, `diffuse/actions/02-render.md:77`. Renommer une section casse jusqu'à 5 renvois | Énoncer la limite « brief seul = pas de gate de fidélité » une seule fois, dans `references/gate-natures.md`, et y faire pointer toutes les actions | S |
| 🟡 | architecture | `plugins/design/adapters/measure/measure.py:160` | Module fourre-tout de 1087 lignes qui regroupe : snippets JS (160-310), normalisation des couleurs (417-578), cascade et login éditeur (579-685), mesure (686-773), registre d'écarts (774-876), couverture (877-933), verdict (934-1040), CLI | Découper en `colors.py`, `ownership.py`, `ledger.py` et `verdict.py`, et garder `measure.py` comme orchestrateur + CLI. Les tests visent déjà ces frontières | L |
| 🟡 | architecture | `plugins/design/adapters/harness/harness.py:244-555` | Le template HTML/CSS/JS (~310 lignes) est inline. La détection 1.x / `release.json` est réécrite dans `harness.py:597-615`, `tools/generate.py:484`, `tools/run-gates.py:119-126` et `tools/status.py:62` | Sortir le template dans `adapters/harness/template.html`. Créer un module Python unique de chargement du contrat (codes 2/3) importé par les 4 | M |
| 🟡 | architecture | `plugins/design/skills/adjust/actions/02-freeze.md:15-313` | Action de 349 lignes qui cumule 8 rôles : canonisation, écriture des artefacts avec schémas recopiés, audit de policies et bump, a11y/checks/gaps, `oracle.pages` et preuve du gate, retrofit, charte | Extraire « Prouver le gate de fidélité » et « Étape 2bis retrofit » en actions de la table d'`adjust`, et remplacer les schémas par des renvois | M |
| 🟡 | architecture | `plugins/design/skills/enforce/actions/05-fidelity-gate.md:27` | Renvoi au « README » de `adapters/measure/`, qui n'existe pas (aucun fichier suivi ni sur disque). Même chose dans `define/actions/05-copycat-fanout.md:14`. L'installation de l'oracle n'est documentée nulle part côté adapter | Créer `adapters/measure/README.md` (venv, `requirements.txt`, `playwright install chromium`, modes A/B), ou renvoyer vers `references/wireframe-render-setup.md` | S |
| 🟢 | architecture | `plugins/design/agents/copycat.md:25` | copycat se dit chargé « par `define` » seulement, alors que sa description (`:3`) et `README.md:17` citent aussi `enforce`. Le fichier porte deux rôles, bulk (125-228) et drift (230-290). La mémoire (`aidd_docs/memory/design-plugin.md:214`) cite `adapters/lint-core.mjs` au lieu de `skills/enforce/adapters/lint-core.mjs` | Corriger la ligne 25 et le chemin de la mémoire. Scinder bulk/drift si le fichier grossit | S |

## Top actions

1. Corriger `funnel-map.md` (ligne 1), la seule carte restituée à l'utilisateur, et la garder par `design-behave`. Patch S, immédiat.
2. Un exemplaire unique par schéma, rangé dans `references/` (lignes 3-5), et `lint-core.mjs` sorti vers `tools/` (ligne 6). Un plan court via `aidd-dev:01-plan`.
3. Sortir WordPress/FSE du cœur `adapters/measure` vers `sc-php` (ligne 2), puis découper `measure.py`/`harness.py` (lignes 8-9). Ce lot est L, donc à planifier.

## Coverage

- **Scanned**: architecture.
  - Conformité vérifiée contre `aidd_docs/memory/design-plugin.md`, `plugins/design/README.md`, `docs/concepts.md`, `docs/workflow.md`, `references/*.md`, `skills/*/SKILL.md`, `skills/detail/references/funnel-map.md` et `agents/copycat.md`.
  - Tous les liens markdown relatifs se résolvent (vérifié par script).
  - La dérive des manifestes `.claude-plugin`/`.codex-plugin` et des copies de `lint-core.mjs` est gardée (`tools/eval/consistency.mjs:54-63`, `design-behave.mjs:143-153`).
- **Skipped**: pas d'ADR ni de diagramme C4 dans le plugin : la conformité s'appuie sur la mémoire et les docs du plugin. `.claude/rules/plugins-marketplace.md`, cité par le CLAUDE.md global, est introuvable (dossier `.claude/rules/` vide, rien de suivi). Ce point est hors du périmètre `plugins/design/`, mais il est à signaler.
