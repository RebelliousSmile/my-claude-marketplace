# Review — chapter-02 — persona: SRE on call

- ✅ Terminal states are bolded and the retryable column answers the 3am question.
- ⚠️ Missing: which error codes map to `failed`. Add an error-code table.
- ⚠️ Webhook retry schedule is qualitative ("exponential backoff") — give the
  actual intervals so on-call knows when to stop waiting.
