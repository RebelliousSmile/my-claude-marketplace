# Style — Scannable tables

- Prefer tables over paragraphs for parameters, states, and error codes.
- Bold the terminal states.
- Each section opens with a one-line summary an on-call engineer can grep.
