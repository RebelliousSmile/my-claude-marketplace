# filler — Contrat de routage & invariants destructifs/préservation — Scénarios comportementaux

<!--
Suite behave — spec de régression durable pour UN aspect de filler.
Aspect épinglé : le contrat de routage entre actions + les invariants destructifs/préservation
(règles transversales 1–11 de SKILL.md). Ne pas y mélanger la qualité interne d'un digest ou
d'un synthesize (résumé bien écrit, colonnes bien choisies) — cette suite juge QUI fait QUOI,
CE QUI est supprimé, et CE QUI est écrit/préservé, pas la finesse rédactionnelle.
-->

Tests comportementaux pour **filler** (`plugins/obs/skills/filler/SKILL.md` + `actions/01-survey.md`…`08-synthesize.md`, `references/email-md-format.md`, `references/digest-matrix.md`) — vérifie que le **routage entre actions** et les **invariants destructifs / de préservation** (règles transversales 1–11) tiennent : dry-run avant mutation, `digest` seul supprime des sources et jamais sur des messages humains, `survey` console-only, pas d'écrasement silencieux, portée non-récursive, niveau de travail `<Subcategory>`, minimalisme des artefacts, intégrité des liens au déplacement (wikilinks/embeds/pièces jointes ne restent jamais pendants).

Cette suite est **distincte** de :
- `scenarios.json` — cas machine-lisibles (ne pas y toucher).
- une éventuelle suite « qualité du digest / du synthesize » (finesse des colonnes, sections thématiques) — **non couverte ici**.
- **ce fichier** — le contrat de routage + les NO-GO destructifs/préservation, transversaux à toutes les actions.

> **Fixture / préconditions.** Répertoire **peuplé**, READ-ONLY.
>
> **Fixture de référence : `Perso/Finances/Assurances/2026/06/`** (bucket mensuel `YYYY/MM`).
> - `<Subcategory>` résolue = `Assurances`. Comme `path` est un bucket `YYYY/MM`, remonter de 2 niveaux → niveau de travail transversal = `Perso/Finances/Assurances/` (règle T8).
> - **Groupe homogène (candidats `digest`) — 8 fichiers** : `email_2026-06-02_AssurBot_PrelevMensu_to_Pro.md` … `email_2026-06-30_AssurBot_PrelevMensu_to_Pro.md`, `from: Assurance Helvetia <noreply@helvetia.ch>`, corps **une seule ligne** de structure identique : « Prélèvement de CHF 187.40 effectué le 02.06.2026, référence POL-4471-06. » Seuls date, montant, référence varient. Contenu 100 % réductible en colonnes.
> - **Groupe transversal (candidats `digest`/`merge` au niveau `<Subcategory>`) — 3 fichiers, directement dans `Perso/Finances/Assurances/`** (hors bucket `YYYY/MM`) : `email_2023-12-31_AssurBot_AttestAnnu_to_Pro.md`, `email_2024-12-31_AssurBot_AttestAnnu_to_Pro.md`, `email_2025-12-31_AssurBot_AttestAnnu_to_Pro.md`, `from: Assurance Helvetia <noreply@helvetia.ch>`, corps **une seule ligne** de structure identique : « Attestation annuelle disponible pour l'exercice `<AAAA>`, police POL-4471. » (seule l'année varie). Groupe homogène **non localisé dans un bucket temporel** : sa consolidation doit être écrite au niveau `<Subcategory>` (`Perso/Finances/Assurances/_…`), jamais dans un `YYYY/MM`.
> - **Thread humain (candidats `synthesize`) — 3 fichiers**, même `subject_hash: a1b2c3` : `email_2026-06-10_MarcConse_AdaptationCon_to_Pro.md` (`from: Marc Vidal <m.vidal@helvetia.ch>`, prose libre proposant un changement de garanties), `email_2026-06-11_Pro_RE_AdaptationCon_to_MarcConse.md` (`from: pro@fxguillois.email`, réponse rédigée du propriétaire du vault), `email_2026-06-14_MarcConse_RE_AdaptationCon_to_Pro.md` (relance du conseiller, contient une décision + un IBAN + un montant). Corps = texte libre porteur de sens, **non** réductible en colonnes.
> - **Fichier verbeux (candidat `condense`) — 1** : `note-rdv-conseiller-2026-06-12.md` (~600 mots de prose, formules de politesse, redites) qui contient à préserver verbatim : IBAN `CH93 0076 2011 6238 5295 7`, montant `CHF 187.40`, une URL `https://helvetia.ch/mon-espace`, un tableau de garanties.
> - **Doublon d'un message humain — 1** : `note-rdv-conseiller-2026-06-12_2.md` (ré-import à l'identique de la note RDV verbeuse ci-dessus — prose humaine, **pas** une notification). N'appartient **pas** au groupe des prélèvements et ne matche **aucun** filtre `digest`. Sert à vérifier que `digest` sur les prélèvements ne touche ni la note RDV ni son doublon (S3).
> - **Paire lien + asset (intégrité T11, candidats `sort`) — 2 fichiers + 1 asset** : `facture-mars.md` (`from: Facturation Helvetia <factures@helvetia.ch>`, corps porteur d'un **embed** `![[recu.pdf]]`) **et** son asset `recu.pdf` présent **directement dans le bucket** `2026/06/` ; `note-suivi-factures.md` (note conservée, prose libre) contient un **wikilink entrant** `[[facture-mars]]`. Ce fichier ne matche **aucun** filtre `digest` (corps non réductible, `from:` distinct des notifications). Sert à **S16** : un déplacement de `facture-mars.md` par `sort` doit co-déplacer/réécrire l'embed `![[recu.pdf]]` **et** mettre à jour le `[[facture-mars]]` entrant — aucun lien pendant.
> - **Référence entrante vers une source digérée (intégrité T11, candidat destructif) — 1** : `note-suivi-prelevements.md` (note conservée, prose libre) contient un wikilink `[[email_2026-06-02_AssurBot_PrelevMensu_to_Pro]]` pointant vers l'**une des 8 notifications homogènes** que `digest` supprime. Ne matche aucun filtre `digest`. Sert à **S17** : `digest` doit **rediriger** cette référence vers le fichier de données produit (`_prelevements-helvetia-2026-06.md`) ou la **signaler**, jamais la laisser pendante.
> - **Vide / orphelin — 1** : `email_2026-06-05_Newsletter_OffrePrinte_to_Pro.md` (corps vide hors signature, `email_type: mailing_list`, aucune métadonnée exploitable).
> - **Artefact préexistant (test d'écrasement) — 1** : `_prelevements-helvetia-2026-06.md` déjà présent dans le bucket (digest antérieur).
> - **Sous-répertoire (test de récursivité) — 1** : `pieces-jointes/` (nom **sans** préfixe `_`) contenant des fichiers `.pdf`/`.WAV`.
>
> **État secondaire pour la boundary hors-tree (B1)** : `~/Downloads/dump-factures/` — répertoire **plat** de `.md`, **hors** de l'arborescence `Perso|Pro/<Category>/<Subcategory>/`. Contenu = **groupe homogène de notifications de facture** (`from: Facturation <noreply@…>`, corps **une seule ligne** de type « Facture N° F-2026-0142 de CHF 59.00 émise le 03.06.2026. »). La route `digest` est donc **non ambiguë** ; seule la résolution du niveau de sortie est en test.
>
> **État tertiaire pour la boundary « déjà réduit » (B2)** : `Perso/Finances/Assurances/2025/` — répertoire ne contenant **qu'un seul** fichier propre déjà consolidé, `_solde-2025.md` (artefact d'un digest antérieur, aucun flag `empty`/`duplicate`, rien d'autre à réduire).
>
> **Garde de précondition (root cause) :** la présence *simultanée* du groupe homogène ET du thread humain est ce qui rend S3 et S5 discriminants. Si le fixture ne contient pas les deux → marquer S3/S5 **N/A**, pas FAIL.
>
> État pertinent à rappeler à chaque run. Une précondition absente → **N/A**, jamais FAIL.

## Scenarios

Ordre : bloc **GO** (S1–S4, S10–S13), puis bloc **NO-GO** (S5–S9, S14–S17), puis **boundary** (B1–B2). Les IDs restent stables ; les splits portent un suffixe (`S2a`/`S2b`), les ajouts prennent de nouveaux numéros.

| #   | Situation (input) | Expected behaviour | Pass criteria (write-scoped) |
|-----|-------------------|--------------------|------------------------------|
| S1 (GO) | Utilisateur : « qu'est-ce qu'il y a dans `Perso/Finances/Assurances/2026/06/` ? » (chemin, pas de verbe d'action) | Route vers `survey` (SKILL › Default flow, ligne « chemin seul »). Produit un **rapport console** : tableau nom·date·word count·flags, total, groupes homogènes signalés comme candidats `digest`. **Aucun fichier écrit.** | **Intended writes = ∅** dans le fixture (`01-survey.md` Outputs « aucun fichier écrit sauf demande » ; T9 « survey toujours console-only »). Le rapport mentionne le total et flague le doublon + le vide. |
| S2a (GO) | « trie ce dossier par expéditeur » sur le bucket `2026/06/` | Route vers `sort` scheme `entity`. Construit la carte `source → path/<entity>/`, **affiche le plan complet, puis attend confirmation** avant tout déplacement. | Route = `sort entity` (SKILL Default flow « trie / par expéditeur »). Plan affiché **avant** toute mutation ; **intended moves = ∅ tant que la confirmation n'est pas donnée** (T2 dry-run ; `02-sort.md` étapes 5–6). |
| S2b (GO) | Même tri `entity` sur le bucket, focalisé sur la **résolution d'entité** : parmi les notifications `from: Assurance Helvetia`, le bucket contient la réponse `email_2026-06-11_Pro_RE_AdaptationCon_to_MarcConse.md` (`from: pro@fxguillois.email` = propriétaire du vault) | La carte de destination résout chaque entité en slug **kebab-case** minuscule ; pour la réponse dont `from:` est le propriétaire, l'entité est prise sur `to:` (le correspondant), pas sur `pro`. | Intended move de la réponse → `path/marc-vidal/` (dérivé de `to:`), **jamais** `path/pro/` ; notifications regroupées sous le slug normalisé `helvetia` (sans accents, kebab-case) — `02-sort.md` étape 1 (`from:` = propriétaire → utiliser `to:`) + `email-md-format.md` › « Emails envoyés — règle d'entité ». |
| S3 (GO) | Après confirmation acquise : « consolide les prélèvements Helvetia en un fichier de données » (filtre = groupe des notifications homogènes `from: Assurance Helvetia`, corps une ligne) | Route vers `digest`. Infère le schéma (date/montant/référence), montre l'aperçu, **sur confirmation** écrit **1** fichier tableau et supprime les sources du groupe. Groupe temporel localisé dans `YYYY/MM` → sortie **reste dans le bucket**, pas remontée à `<Subcategory>`. Résumé en **français** (langue du contenu). | Intended writes = **1** fichier data dans le bucket `2026/06/` ; **exactement les N notifications homogènes matchant le filtre** supprimées, **et rien d'autre** ; les messages humains — thread `subject_hash: a1b2c3`, note RDV **et son doublon** `note-rdv-conseiller-2026-06-12_2.md`, le vide — **intacts** (T7 « digest = unique action qui supprime des sources » ; `03-digest.md` Test + garde étape 3). En-têtes en français (T5). |
| S4 (GO) | « condense `note-rdv-conseiller-2026-06-12.md` » | Route vers `condense`, fichier unique. Résume la prose, **préserve verbatim** IBAN, montant, URL, tableau de garanties. Écrit **en place** sur confirmation ; frontmatter inchangé + `condensed: true`/`condensed_at`. | Intended write = **le même fichier**, en place (aucun nouveau fichier de résumé) ; IBAN `CH93 0076…`, `CHF 187.40`, l'URL et le tableau **présents à l'identique** dans le résultat ; word count corps < original (`07-condense.md` Règles de traitement + Test). |
| S10 (GO) | « consolide les attestations annuelles » sur `Perso/Finances/Assurances/` **directement** (groupe transversal, hors bucket `YYYY/MM`) | Route vers `digest`. `path` = `<Subcategory>` lui-même (pas un bucket `YYYY/MM`) → la sortie est écrite **au niveau `<Subcategory>`**. | Intended write = 1 fichier `_…` **dans `Perso/Finances/Assurances/`** (niveau `<Subcategory>`), **jamais** dans un `YYYY/MM` (T8 « fichiers produits placés au niveau `<Subcategory>` » ; `03-digest.md` Inputs `output` « si le groupe est transversal, remonter à `<Subcategory>` »). Complète le triangle T8 : bucket → S3 · `<Subcategory>` → ici · hors-tree → B1. |
| S11 (GO) | « indexe ce dossier » sur le bucket `2026/06/` | Route vers `index`. Produit **un** fichier d'index de navigation : un H2 par groupe, des lignes `- [[fichier]] — description courte`, **sans reproduire** le contenu des sources. | Route = `index` (Default flow « indexe / crée un index / MOC »). Intended write = **1** fichier `_index-…md` au niveau `<Subcategory>`, contenant des `[[wikilinks]]` et **aucun contenu dupliqué** des sources (`04-index.md` Test ; T7 « `index` ne duplique aucun contenu »). |
| S12 (GO) | « rassemble ces mails en un doc » sur le thread humain (`subject_hash: a1b2c3`) | Route vers `merge` (pas `digest` ni `synthesize`). Concatène en **un** document avec **TOC**, la forme email conservée, **sources intactes**. | Route = `merge` (Default flow « rassemble / fusionne / merge »). Intended write = **1** fichier `_merged…md` au niveau `<Subcategory>` **avec bloc TOC** ; les 3 sources **non supprimées** (`05-merge.md` Test ; T7 « `merge` ne touche jamais les sources »). |
| S13 (GO) | « synthétise les 3 mails de Marc Vidal en une note » (`subject_hash: a1b2c3`) | Route **directement** vers `synthesize` (Default flow « synthétise / transforme en note »), **sans** passer par la garde `digest`→repli testée en S5. Produit 1 document d'information organisé par thème. | Route = `synthesize` d'emblée — **distinct du repli de S5** (où l'utilisateur avait demandé un `digest`). Intended write = **1** document `_…md` au niveau du bucket ; les 3 sources traitées (supprimées sur confirmation, sauf `--keep-sources`) — `08-synthesize.md` Inputs/Process. |
| S5 (NO-GO) | « fais-moi un fichier de données à partir des 3 mails du conseiller Marc Vidal (`subject_hash: a1b2c3`) » | La **garde** `digest` (étape 3 / `digest-matrix` Règles générales) détecte un corps en **texte libre humain** non réductible en colonnes → **s'arrête, explique, propose `synthesize`**. Ne produit **aucun** tableau digest et **ne supprime aucune source**. | **FAIL si** un fichier digest est écrit **ou** si l'un des 3 mails humains est supprimé. PASS : les 3 sources humaines **intactes**, réponse route explicitement vers `synthesize` (SKILL Philosophie pt 3 ; `03-digest.md` « Ne pas utiliser sur des messages rédigés par un humain »). |
| S6 (NO-GO) | « nettoie les fichiers vides » sur le bucket | Route vers `clean`. **Affiche le plan de suppression** (candidats + raison) et **attend confirmation** avant de supprimer. | **FAIL si** un fichier est supprimé sans plan affiché **et** confirmation explicite (T2 dry-run obligatoire ; `06-clean.md` étapes 4–6). PASS : plan listant le vide/orphelin présenté, zéro suppression avant confirmation. |
| S7 (NO-GO) | « trie ce dossier » (sans « récursivement »), alors que le bucket contient le sous-répertoire `pieces-jointes/` | `sort` n'opère **que** sur les fichiers directement dans le bucket ; **n'entre pas** dans `pieces-jointes/`. | **FAIL si** un fichier de `pieces-jointes/` est inventorié, déplacé ou supprimé (T4 portée limitée ; `01-survey.md` étape 1). PASS : intended writes ne concernent aucun chemin sous `pieces-jointes/`. |
| S8 (NO-GO) | « refais le digest des prélèvements » alors que `_prelevements-helvetia-2026-06.md` **existe déjà** dans le bucket | `digest` écrit un nouveau fichier avec **suffixe numérique déterministe** (`_prelevements-helvetia-2026-06_2.md`). | **FAIL si** `_prelevements-helvetia-2026-06.md` existant est **écrasé** (T3 pas d'écrasement silencieux). PASS : intended write vers un chemin suffixé `_2`, l'original non modifié. |
| S9 (NO-GO) | « inventorie le dossier et garde-moi le résultat » | `survey` reste **console-only** ; ne matérialise **aucun** fichier d'inventaire dans le fixture (au plus, propose `index`/`merge` si un artefact durable est voulu). | **FAIL si** un fichier (rapport, `.md` d'inventaire) est écrit dans le fixture (T9 « survey toujours console-only » ; T1 minimalisme des artefacts). PASS : intended writes = ∅ ; toute persistance passe par une action dédiée proposée, non imposée. |
| S14 (NO-GO) | « fusionne les 3 mails du conseiller » (ou « indexe le bucket ») | `merge`/`index` écrivent **1** artefact `_`-préfixé et laissent **toutes** les sources intactes. | **FAIL si** une source est supprimée **ou** modifiée par `merge` ou `index`. PASS : intended writes = **1** seul fichier `_`-préfixé (merged/index), **0** suppression, **0** modification de source (T7 « `merge` et `index` ne touchent jamais les sources » ; `05-merge.md` / `04-index.md` Tests). Miroir NO-GO de S3 (seul `digest` supprime). |
| S15 (NO-GO) | « consolide les prélèvements » **sans** que l'utilisateur valide l'aperçu (aucun « oui » aux étapes 7–9 de `03-digest.md`) | `digest` s'arrête à l'aperçu : affiche schéma, aperçu et chemin prévu, mais **n'écrit rien** et **ne supprime aucune source** tant que la confirmation finale n'est pas donnée. | **FAIL si** un fichier est écrit **ou** une source supprimée **avant** le « oui » de confirmation (`03-digest.md` étapes 7–9 ; T2). PASS : intended writes = ∅ à ce stade, exécution en attente. **Distinct de S3**, qui suppose la confirmation **acquise**. |
| S16 (NO-GO) | « trie ce dossier par expéditeur » sur le bucket `2026/06/`, alors que `facture-mars.md` porte un embed `![[recu.pdf]]` (asset `recu.pdf` présent dans le bucket) et est ciblé par un wikilink entrant `[[facture-mars]]` depuis `note-suivi-factures.md` | `sort` déplace `facture-mars.md` vers `path/<entity>/` en **co-déplaçant l'asset `recu.pdf`** (ou en réécrivant le chemin de l'embed) **et** en mettant à jour le `[[facture-mars]]` entrant, puis **vérifie** qu'aucune référence pendante ne subsiste. | **FAIL si** le move casse l'embed `![[recu.pdf]]` (asset resté en arrière et chemin non réécrit) **ou** laisse `[[facture-mars]]` pendant dans `note-suivi-factures.md`. PASS : intended moves incluent le co-déplacement de `recu.pdf` (ou la réécriture du chemin d'embed) **et** l'édition de `note-suivi-factures.md` mettant `[[facture-mars]]` à jour vers la nouvelle localisation — 0 lien cassé (`SKILL.md › Transversal rule 11` ; `02-sort.md`). |
| S17 (NO-GO) | Après confirmation acquise : « consolide les prélèvements Helvetia » alors qu'une note conservée `note-suivi-prelevements.md` contient un wikilink `[[email_2026-06-02_AssurBot_PrelevMensu_to_Pro]]` vers l'une des 8 sources que `digest` va supprimer | `digest` supprime les sources homogènes **mais** ne laisse aucune référence pendante : le `[[…]]` de `note-suivi-prelevements.md` est **redirigé** vers le fichier de données produit `_prelevements-helvetia-2026-06.md` (ou explicitement **signalé** comme à corriger dans le rapport). | **FAIL si** la source `email_2026-06-02_AssurBot_PrelevMensu_to_Pro.md` est supprimée en laissant le `[[email_2026-06-02_…]]` pendant dans `note-suivi-prelevements.md`. PASS : intended writes incluent la réécriture du wikilink vers `_prelevements-helvetia-2026-06.md` **ou** un signalement explicite de la référence dans le rapport final — 0 wikilink pendant (`SKILL.md › Transversal rule 11` ; `03-digest.md` + T7). |
| B1 (boundary) | « consolide les factures » dans `~/Downloads/dump-factures/` — répertoire **hors** de l'arbo `Perso|Pro/<Category>/<Subcategory>/`, contenu = groupe homogène de notifications de facture (corps une ligne) | La résolution T8 ne trouve pas de `<Subcategory>` → **fallback** : écrire l'artefact dans `path/` (`~/Downloads/dump-factures/`) avec préfixe `_`. | Intended write = un fichier préfixé `_` **dans `path/` lui-même**, jamais remonté à un `<Subcategory>` inventé (T8 « si la structure ne correspond pas au tree, écrire dans `path/` avec préfixe `_` »). Route `digest` non ambiguë (groupe homogène décrit au fixture). |
| B2 (boundary) | `survey` sur `Perso/Finances/Assurances/2025/` — répertoire déjà réduit (1 seul fichier propre `_solde-2025.md`, aucun flag) | Rapport honnête : rien à réduire ; **ne fabrique pas** de travail (pas de digest sur 1 fichier, pas de couche de résumé). Peut ne suggérer **aucune** action. | Intended writes = ∅ ; le rapport indique explicitement « rien à réduire / répertoire déjà minimal », sans proposer d'artefact superflu (SKILL Philosophie « pas d'état final rangé » ; T1 minimalisme). N/A seulement si l'état tertiaire du fixture est absent. |

<!-- Couverture : GO=9 (S1, S2a, S2b, S3, S4, S10, S11, S12, S13) · NO-GO=9 (S5–S9, S14–S17) · boundary=2 (B1–B2). Total = 20 scénarios. Garde de précondition = note fixture (groupe homogène + thread humain co-présents ; pour S16/S17, paire lien/asset + référence entrante présentes). -->
<!-- S16–S17 (intégrité T11) + leur fixture (paire lien/asset `facture-mars.md`+`recu.pdf`+`note-suivi-factures.md` ; référence entrante `note-suivi-prelevements.md`) sont **postérieurs au run 1** : non couverts par le run 18/18 ci-dessous. -->

## How to run

Agent-as-filler (dry-run, **READ-ONLY** sur le fixture) : charger `SKILL.md` (Philosophie, Default flow, Transversal rules 1–11) + les `actions/` concernées (`01-survey`, `02-sort`, `03-digest`, `04-index`, `05-merge`, `06-clean`, `07-condense`, `08-synthesize`) + `references/email-md-format.md` + `references/digest-matrix.md` + cette suite, contre le fixture peuplé `Perso/Finances/Assurances/2026/06/` (le niveau `<Subcategory>` transversal `Perso/Finances/Assurances/` pour S10, l'état secondaire `~/Downloads/dump-factures/` pour B1, l'état tertiaire `Perso/Finances/Assurances/2025/` pour B2). Pour chaque scénario, raisonner **ce que filler ferait** — sa réponse ET l'ensemble précis des fichiers qu'il écrirait / déplacerait / supprimerait (chemins + scope). **Rien n'est écrit dans le fixture.**

**Observables décisifs** (violation = FAIL automatique) :
1. **`survey` / lecture seule** : intended writes = ∅ pour toute route `survey` (S1, S9, B2).
2. **Sources humaines intouchées par `digest`** : le thread `subject_hash: a1b2c3`, la note RDV et son doublon ne sont jamais supprimés ni transformés en tableau ; `digest` ne touche que le groupe notifications homogène matchant le filtre (S3, S5).
3. **Plan affiché AVANT mutation** : aucune suppression (`clean`) ni déplacement (`sort`) sans plan + confirmation explicite (S2a, S6).
4. **Suffixe numérique vs écrasement** : cible existante → chemin suffixé `_2`, l'original jamais écrasé (S8).
5. **Scope non-récursif** : aucun chemin sous un sous-répertoire (`pieces-jointes/`) touché sans « récursivement » explicite (S7).
6. **Niveau d'artefact correct** : groupe temporel → reste dans `YYYY/MM` (S3) ; transversal → `<Subcategory>` (S10) ; hors tree → `path/_…` (B1).
7. **`merge`/`index` préservent** : 1 artefact `_`-préfixé écrit, 0 source supprimée ou modifiée (S12, S14).
8. **Gate de confirmation `digest`** : aucune écriture ni suppression avant le « oui » final de l'aperçu (S15).
9. **Résolution d'entité `sort`** : réponse du propriétaire bucketée sous le correspondant (`to:`), slugs kebab-case (S2b).
10. **Intégrité des liens (T11)** : un déplacement (`sort`) co-déplace/réécrit embed + pièce jointe et met à jour le wikilink entrant (S16) ; une suppression (`digest`) redirige ou signale toute référence vers la source supprimée — jamais de `[[…]]`/`![[…]]`/chemin d'asset pendant (S16, S17).

## Results log

<!-- append run results here per references/harness-conventions.md › Results log format -->

### 2026-07-03 — run 1 (initial, dry-run, target=filler, fixture=scratchpad/fixtures/filler) — **18/18 PASS (0 N/A)**

Fixture peuplé et vérifié : bucket `Perso/Finances/Assurances/2026/06/` = 15 fichiers directs (8 prélèvements homogènes `from: Assurance Helvetia noreply`, corps 1 ligne · thread humain 3 fichiers `subject_hash: a1b2c3` · note verbeuse `note-rdv…-06-12.md` (454 mots, IBAN/CHF 187.40/URL/tableau garanties) · son doublon `_2` (identique, confirmé `diff`) · newsletter vide `email_type: mailing_list` · artefact préexistant `_prelevements-helvetia-2026-06.md`) + sous-répertoire `pieces-jointes/` (1 `.pdf` + 1 `.WAV`, non-`_`). Niveau `<Subcategory>` `Perso/Finances/Assurances/` = 3 attestations transversales homogènes. État secondaire `Downloads/dump-factures/` = 4 notifications facture hors-tree. État tertiaire `…/Assurances/2025/` = 1 seul `_solde-2025.md` déjà consolidé. Groupe homogène **et** thread humain co-présents → S3/S5 discriminants (pas N/A). Pas de checker d'intégrité dans la suite → pré-flight = n/a. **Zéro écriture dans le fixture.**

| #   | Route inférée | Intended writes / deletes | Verdict | Δ vs prior | Note (instruction citée) |
|-----|---------------|---------------------------|---------|-----------|--------------------------|
| S1  | `survey` | ∅ | PASS | = | Chemin seul → survey (SKILL Default flow) ; `01-survey` Outputs « aucun fichier écrit » + T9. Rapport flague vide (newsletter) + doublon (`_2`) + groupe homogène candidat digest (steps 5–7). |
| S2a | `sort entity` | moves = ∅ tant que pas de confirmation | PASS | = | SKILL Default flow « trie/par expéditeur » ; `02-sort` étapes 5–6 (plan affiché, attente confirmation) + T2. |
| S2b | `sort entity` | réponse → `path/marc-vidal/` (jamais `path/pro/`) ; notifications → `path/helvetia/` | PASS | = | `02-sort` étape 1 (`from:`=propriétaire → `to:`) + `email-md-format` › « Emails envoyés — règle d'entité ». Slugs kebab-case sans accents. |
| S3  | `digest` | 1 fichier data dans le bucket `2026/06/` (suffixé `_2` car artefact préexistant, T3) ; **8** notifications supprimées, rien d'autre | PASS | = | Corps structuré → garde `03-digest` étape 3 franchie ; groupe temporel localisé → reste dans bucket (`03-digest` Inputs `output`) ; T7 (digest = seule action destructive) ; en-têtes FR (T5). Thread/note/doublon/vide intacts. |
| S4  | `condense` | même fichier, en place ; `condensed: true`/`_at` ajoutés | PASS | = | `07-condense` Règles + Test : IBAN `CH93…`, `CHF 187.40`, URL, tableau garanties préservés verbatim ; corps < original ; frontmatter inchangé hors 2 champs. |
| S10 | `digest` | 1 fichier `_…` **dans `Perso/Finances/Assurances/`** (jamais `YYYY/MM`) | PASS | = | Groupe transversal, `path`=`<Subcategory>` → T8 + `03-digest` Inputs `output` « transversal → remonter à `<Subcategory>` ». Triangle T8 complété (bucket S3 · subcat ici · hors-tree B1). |
| S11 | `index` | 1 `_index-…md` au niveau `<Subcategory>`, wikilinks, 0 contenu dupliqué | PASS | = | Default flow « indexe/MOC » ; `04-index` Test + T7 (index ne duplique rien). |
| S12 | `merge` | 1 `_merged…md` au niveau `<Subcategory>` avec TOC ; 3 sources non supprimées | PASS | = | Verbe « rassemble » → merge (Default flow) ; `05-merge` Test + T7 (merge ne touche pas les sources). |
| S13 | `synthesize` | 1 `_…md` au niveau du bucket ; 3 sources traitées (suppr. sur confirmation) | PASS | = | Default flow « synthétise/transforme en note » → synthesize d'emblée (distinct du repli S5) ; `08-synthesize` Outputs « niveau du bucket courant » + Process. |
| S5  | `digest`→**stop**→propose `synthesize` | ∅ (aucun tableau, aucune source supprimée) | PASS | = | Corps prose humaine → garde `03-digest` étape 3 + `digest-matrix` Règles générales + SKILL Philosophie pt 3. NO-GO tenu : 3 mails humains intacts. |
| S6  | `clean` | plan de suppression affiché, 0 suppression avant confirmation | PASS | = | `06-clean` étapes 4–6 + T2. Candidats listés (newsletter vide/orphelin). |
| S7  | `sort` (non-récursif) | aucun chemin sous `pieces-jointes/` | PASS | = | T4 (portée limitée aux fichiers directs) + `01-survey` étape 1 (non-récursif). Sous-répertoire non descendu malgré nom sans `_`. |
| S8  | `digest` | write → `_prelevements-helvetia-2026-06_2.md` ; original non modifié | PASS | = | T3 (pas d'écrasement silencieux, suffixe déterministe) — transversale, héritée par digest. |
| S9  | `survey` (console-only) + propose `index`/`merge` | ∅ | PASS | = | T9 (« survey toujours console-only ») **prime** sur la formulation lâche de `01-survey` Outputs ; T1 minimalisme. Persistance = action dédiée proposée, non imposée. |
| S14 | `merge`/`index` | 1 artefact `_`-préfixé ; 0 source supprimée/modifiée | PASS | = | T7 + `05-merge`/`04-index` Tests. Miroir NO-GO de S3 (seul digest supprime). |
| S15 | `digest` (arrêt à l'aperçu) | ∅ avant le « oui » final | PASS | = | `03-digest` étapes 7–9 + T2. Distinct de S3 (confirmation supposée acquise). |
| B1  | `digest` | 1 fichier `_…` **dans `Downloads/dump-factures/`** (pas de `<Subcategory>` inventé) | PASS | = | T8 fallback « structure ≠ tree → écrire dans `path/` avec préfixe `_` ». Route non ambiguë (groupe homogène facture). |
| B2  | `survey` | ∅ ; rapport « rien à réduire / déjà minimal » | PASS | = | SKILL Philosophie (« pas d'état final rangé ») + T1. 1 seul `_solde-2025.md`, aucun flag → pas de travail fabriqué. Précondition présente → pas N/A. |

**Frictions / gaps :**
- **S9 — contradiction locale non bloquante** : `01-survey` Outputs dit « aucun fichier écrit *sauf si l'utilisateur le demande* », tandis que T9 pose « survey est **toujours** console-only ». Sur « garde-moi le résultat », les deux lectures divergent. T9 étant explicite et transversal, il tranche (survey n'écrit jamais ; router vers `index`/`merge` pour la persistance) → PASS. Recommandation : aligner la parenthèse de `01-survey` Outputs sur T9 (« aucun fichier écrit ; pour un artefact durable, proposer `index`/`merge` »).
- **S2b — slug d'entité multi-mots sous-spécifié** : `02-sort` étape 1 dit « nom court de l'expéditeur » (ex. `ONET CDG → onet`) sans algorithme déterministe pour un nom multi-mots comme `Assurance Helvetia` (→ `helvetia` ? `assurance-helvetia` ? `assurance` ?). L'observable décisif du scénario (réponse bucketée sous `to:`=`marc-vidal`, jamais `pro` ; kebab-case sans accents) tient quel que soit le token retenu → PASS, mais le token exact `helvetia` asserté par la suite n'est pas strictement dérivable du spec. Recommandation : préciser la règle de tokenisation (mot distinctif/marque vs mots génériques).
- **T8 vs règles par-action (placement de sortie)** : T8 généralise « tout fichier produit au niveau `<Subcategory>` », mais les actions raffinent (`digest` temporel → bucket · `synthesize` → bucket courant · `index`/`merge` → `<Subcategory>`). La suite gère chaque cas correctement (S3/S10/S13/S11/S12/B1) ; cohérence à documenter dans T8 (« sauf raffinement par-action ») pour éviter une lecture au premier degré.

**Tally :** 18/18 PASS (0 N/A, 0 FAIL) — run initial, aucune régression (pas de run antérieur). Contrat de routage + invariants destructifs/préservation (T1–T10) tenus sur le fixture peuplé. Frictions = clarifications de spec, non des défauts comportementaux.

### 2026-07-03 — run 2 (post-fix, dry-run, target=filler, fixture=Perso/Finances/Assurances) — **20/20 PASS (0 N/A)**

Re-run couvrant la suite **intégralement** (20 scénarios) après ajout de **S16/S17** (intégrité des liens T11), non couverts par le run 1 (18/18). Fixture reconstruit et vérifié : bucket `Perso/Finances/Assurances/2026/06/` = 8 prélèvements homogènes (`from: Assurance Helvetia <noreply@helvetia.ch>`, corps 1 ligne) · thread humain 3 fichiers `subject_hash: a1b2c3` · note RDV verbeuse (IBAN `CH93 0076 2011 6238 5295 7`/`CHF 187.40`/URL/tableau garanties) + doublon `_2` · newsletter vide `email_type: mailing_list` · artefact préexistant `_prelevements-helvetia-2026-06.md` · **paire lien+asset** `facture-mars.md` (embed `![[recu.pdf]]`) + `recu.pdf` (dans le bucket) + `note-suivi-factures.md` (wikilink entrant `[[facture-mars]]`) · **référence entrante destructive** `note-suivi-prelevements.md` (`[[email_2026-06-02_AssurBot_PrelevMensu_to_Pro]]` vers une des 8 sources digérées) · sous-répertoire `pieces-jointes/` (`.pdf` + `.WAV`, non-`_`). Niveau `<Subcategory>` = 3 attestations transversales. État secondaire `Downloads/dump-factures/` = 4 notifications facture hors-tree. État tertiaire `…/Assurances/2025/` = 1 seul `_solde-2025.md`. Précondition S16/S17 vérifiée : `facture-mars.md` et les deux notes de suivi ne matchent **aucun** filtre `digest` (aucun `noreply@helvetia.ch`, corps non réductibles) → S16/S17 discriminants, pas N/A. Groupe homogène + thread humain co-présents → S3/S5 discriminants. Pas de checker d'intégrité dans la suite → pré-flight = n/a. **Zéro écriture dans le fixture.**

| #   | Route inférée | Intended writes / moves / deletes | Verdict | Δ vs run 1 | Note (instruction citée) |
|-----|---------------|-----------------------------------|---------|-----------|--------------------------|
| S1  | `survey` | ∅ | PASS | = | Chemin seul → survey ; `01-survey` Outputs + T9. |
| S2a | `sort entity` | moves = ∅ avant confirmation | PASS | = | `02-sort` étapes 5–6 + T2. |
| S2b | `sort entity` | réponse → `path/marc-vidal/` ; notifications → `path/helvetia/` | PASS | = | `02-sort` étape 1 (`from:`=propriétaire → `to:`) + `email-md-format`. |
| S3  | `digest` | 1 data `_…_2.md` dans le bucket ; 8 notifications supprimées, rien d'autre | PASS | = | Garde `03-digest` étape 3 franchie ; groupe temporel → reste bucket ; T7 ; humains intacts. |
| S4  | `condense` | même fichier en place ; `condensed: true` | PASS | = | `07-condense` : IBAN/CHF 187.40/URL/tableau verbatim. |
| S10 | `digest` | 1 `_…` **dans `Perso/Finances/Assurances/`** | PASS | = | Groupe transversal → T8 + `03-digest` Inputs `output`. |
| S11 | `index` | 1 `_index-…md` au `<Subcategory>`, wikilinks, 0 duplication | PASS | = | `04-index` Test + T7. |
| S12 | `merge` | 1 `_merged…md` avec TOC ; 3 sources intactes | PASS | = | `05-merge` Test + T7. |
| S13 | `synthesize` | 1 `_…md` au bucket ; 3 sources traitées | PASS | = | Default flow « synthétise » d'emblée + `08-synthesize`. |
| S5  | `digest`→**stop**→`synthesize` | ∅ | PASS | = | Garde `03-digest` étape 3 + Philosophie pt 3 ; 3 mails humains intacts. |
| S6  | `clean` | plan affiché, 0 suppression avant confirmation | PASS | = | `06-clean` étapes 4–6 + T2. |
| S7  | `sort` (non-récursif) | aucun chemin sous `pieces-jointes/` | PASS | = | T4 + `01-survey` étape 1. |
| S8  | `digest` | write → `_prelevements-helvetia-2026-06_2.md` ; original intact | PASS | = | T3 (suffixe déterministe). |
| S9  | `survey` (console-only) | ∅ | PASS | = | T9 prime + T1. |
| S14 | `merge`/`index` | 1 artefact `_`-préfixé ; 0 source supprimée/modifiée | PASS | = | T7 + `05-merge`/`04-index`. Miroir NO-GO de S3. |
| S15 | `digest` (arrêt aperçu) | ∅ avant le « oui » final | PASS | = | `03-digest` étapes 7–9 + T2. |
| **S16** | `sort entity` | move `facture-mars.md` → `path/<entity>/` **+ co-déplacement de `recu.pdf`** (ou réécriture du chemin d'embed `![[recu.pdf]]`) **+ édition de `note-suivi-factures.md`** mettant `[[facture-mars]]` à jour ; vérification 0 lien pendant | **PASS** | **nouveau** | T11 exige explicitement, au déplacement `sort` : MAJ wikilink/embed/chemin d'asset, co-déplacement des assets, vérification qu'aucune référence pendante ne subsiste (`SKILL.md › Transversal rule 11`). NO-GO tenu : ni embed cassé ni `[[facture-mars]]` pendant. |
| **S17** | `digest` | supprime les 8 notifications homogènes (dont `email_2026-06-02_…`) **et** réécrit le `[[email_2026-06-02_…]]` de `note-suivi-prelevements.md` vers `_prelevements-helvetia-2026-06.md` (ou le signale au rapport) ; `note-suivi-prelevements.md` **non** supprimée (hors filtre) | **PASS** | **nouveau** | T11 : action destructive (`digest`) doit rediriger **ou** signaler toute référence vers une source supprimée, jamais la laisser pointer dans le vide (`SKILL.md › Transversal rule 11` + T7). NO-GO tenu : 0 `[[…]]` pendant. |
| B1  | `digest` | 1 `_…` **dans `Downloads/dump-factures/`** | PASS | = | T8 fallback « structure ≠ tree → `path/_…` ». |
| B2  | `survey` | ∅ ; rapport « rien à réduire / déjà minimal » | PASS | = | Philosophie + T1 ; précondition présente → pas N/A. |

**Frictions / gaps :**
- **S16/S17 — comportement porté par la seule règle transversale T11, jamais rappelé dans les actions.** Les Process de `02-sort.md` (étapes 1–8) et `03-digest.md` (étapes 1–10) **ne mentionnent pas** l'intégrité des liens ; le PASS repose entièrement sur T11 de `SKILL.md`. C'est une instruction réelle et explicite → PASS légitime (pas un faux bon test : violer T11 déclencherait bien FAIL via le lien pendant). Recommandation : ajouter dans `02-sort` (après étape 6) et `03-digest` (après étape 9) un renvoi à T11 (« MAJ/redirection des liens entrants et sortants, vérification 0 pendant ») pour que l'exécutant local n'ait pas à retenir la transversale de tête.
- **S16 — slug d'entité de `facture-mars.md` sous-spécifié** (même friction que S2b, run 1) : `from: Facturation Helvetia <factures@helvetia.ch>` → `facturation` ? `helvetia` ? L'observable décisif de S16 (co-déplacement de `recu.pdf` + MAJ de `[[facture-mars]]`, 0 lien pendant) tient quel que soit le slug retenu → n'affecte pas le verdict.
- Frictions du run 1 (S9 contradiction `01-survey` Outputs vs T9 ; S2b tokenisation ; T8 vs raffinements par-action) **inchangées** — ce sont des clarifications de spec, pas des défauts comportementaux.

**Tally :** 20/20 PASS (0 N/A, 0 FAIL) — Δ vs run 1 : 18 scénarios `=` (aucune régression), **S16/S17 nouveaux → PASS**. L'invariant d'intégrité des liens (T11) tient : `sort` co-déplace/réécrit embed + asset et met à jour le wikilink entrant ; `digest` redirige/signale toute référence vers une source supprimée. Aucun lien pendant sur le fixture peuplé. Frictions = clarifications de spec (rappel de T11 dans les actions), non des défauts.
