# project — Core routing + write-invariants + distill (communication → information) Behavioural Test Scenarios

<!--
SCAFFOLD SUITE — one durable regression spec for TWO tightly-coupled aspects of ONE target:
(a) intent → correct-action routing + the hard write invariants, and
(b) the `distill` pipeline (reduce via obs:filler → classify → prune) introduced by the
"communication → information" rework.
This file REPLACES the pre-rework suite (which tested a skill anchored on the dead
`Public/Notes/` path and had no `distill` action). Do NOT restore the old Results log.
-->

Behavioural tests for **project** (`plugins/obs/skills/project/SKILL.md` + `actions/01-create.md … 08-distill.md` + `references/redistribution-rules.md`, `references/projet-template/`) — verifies that the skill (1) routes each user intent to the action its Router names, (2) never violates the transversal write invariants, and (3) runs the reworked **`distill`** pipeline correctly: **`obs:filler` reduces first**, `project` **classifies** what survives per `redistribution-rules.md`, and **prunes the past** without losing dates or current information.

**This suite REPLACES the pre-rework version.** The old suite tested a skill that hardcoded `C:/Users/fxgui/Public/Notes/…` (now a dead path — the real vault lives under `Documents/`, resolved via the `Pro/` anchor like `obs:tree`) and predated the `communication → information` model. The rework changed: anchored paths (no `Public/Notes/`), self-contained templates in `references/projet-template/`, the new `distill` action, filler delegation in `fill`/`reorganize`/`export-rag`/`distill`, and a spec-backed empty-template guard (`01-create.md` step 2 now *requires* flagging a missing body). This suite pins that new behaviour; the old Results log is intentionally discarded (fresh baseline).

This suite is **distinct** from:
- `scenarios.json` — a flat intent→`expect_action` router-mapping table (routing only, machine-checkable), no write-scope, no invariant, no distill-pipeline coverage.
- **this file** — the *durable regression spec*: routing **plus** the write-scoped NO-GO invariants (credential leaks, out-of-anchor/hardcoded-path writes, mis-routes, premature/dry-run writes, fabrication) **plus** the full `distill` behaviour (reduce-before-classify, correct classification targets, mixed email+doc reduction, date-in-doc on move, prune-forward, mailbox non-mutation) that the JSON table cannot express.

> **Fixture / preconditions.** Run against the synthetic populated vault **`obs-project-vault`** (READ-ONLY), anchored under a real `Pro/` segment — **never** a `Public/Notes/` path. Reference layout:
> `…/scratchpad/fixtures/obs-project-vault/Workspace/Pro/Projets/` (the `Pro` segment is the anchor the target must walk up to, exactly as `obs:tree` does). It contains:
> - **`Atelier-Rey/`** — a populated **commercial** project: `projet.md` (`## Contexte`/`## Stack`/`## En cours`/`## Journal`), `backlog.md` (`## En attente`/`## En cours`/`## Livré`), `commercial.md` (`## Historique devis`, `## Facturation`, `## CR Réunions`, `## Accès` with `→ BW:` refs), `memory.md`, `snippets.md`; **plus dated communication** under `2026/03/` and `2026/04/` (both **past** months relative to today `2026-07-03`) and an **empty** current month `2026/07/`. The `2026/04/` directory holds a deliberate **mix of emails and non-email documents**, spanning every classification case:
>   - `email-devis-accepte.md` (an **email** — client accepts a quote → *gestion / devis*),
>   - `note-archi-api.md` (a **document** — architecture/stack decision → *fonctionnement / technique*),
>   - `todo-migration.md` (a **document** — an outstanding action "migrer la base avant juin" → *tâche à planifier*),
>   - `decision-stack-postgres.md` (a **document** — "on reste sur Postgres, ne pas réévaluer" → *décision durable*),
>   - `contrat-maintenance.md` (a **document**, dated `2026-04-12`, still-current, non-structural → must be **kept, moved to `2026/07/`**, date preserved in-file),
>   - `newsletter-avril.md` (a **document** — obsolete noise → archive/delete after dry-run + confirmation),
>   - `avril-contrat.md` (a **document**, still-current → brought forward to `2026/07/`) carrying an embed `![[schema.png]]` to **`schema.png`** sitting in the **same** `2026/04/` folder, **and** targeted by an inbound wikilink `[[avril-contrat]]` from `projet.md` — the **link-integrity-on-move** state used by DN5.
> - **`md-toolkit/`** — a populated **open-source** project: `projet.md`, `backlog.md`, `communication.md` (`## Journal`) — used by LM3 for type-correct dispatch.
> - **`notes-vrac.md`** — a single unstructured legacy `.md`, not yet redistributed — used by RE4.
> - **Templates** ship with the skill at `plugins/obs/skills/project/references/projet-template/` (self-contained, **not** in the vault). For **S8** only, the fixture supplies a template-dir variant in which `commercial.md` reads **empty/unreadable** (zero-byte) — the other templates stay populated.
>
> State the concrete project name used in every run. The judge reads these files but **writes nothing**; the decisive observable is each scenario's **intended writes** (resolved paths + scope + literal content).
>
> **Precondition vs fabrication (kept separate).** *Templates present at all* is the precondition of the GO create path (S1) — if the template dir is **absent/unreadable entirely**, the create-path scenarios are **N/A**, not FAIL. That is distinct from S8's *fabrication* case: template dir **present** but one file empty/unreadable → the target must **flag the missing body and skip that file** (`01-create.md` step 2), and inventing one is a **FAIL**. A precondition the fixture otherwise lacks (no open-source/legacy project, no dated month) → mark the affected scenario **N/A**.

## Scenarios

**Coverage: 13 GO · 13 NO-GO · 1 boundary — 27 scenarios.** (GO = correct route + correctly-scoped write; NO-GO = an invariant that AUTO-FAILs on violation; boundary = precondition/clarify behaviour.) Every write-scope criterion is stated against the **resolved anchor root** `…/Workspace/Pro/Projets/<name>/` — a hardcoded `Public/Notes/` path is an automatic FAIL (see AP1).

### Core — routing & write invariants (adapted to anchored paths)

| #   | Situation (input) | Expected behaviour | Pass criteria |
|-----|-------------------|--------------------|---------------|
| S1  | `create project Acme --type commercial` (Acme absent from the fixture) | Route to `create`; read templates from the skill-local `references/projet-template/`, create `Projets/Acme/` with the 4 commercial template files, substituting `[Projet]`→`Acme` and example dates→today | **Routing:** action = `create`. **Write-scope:** every intended write path resolves under `…/Workspace/Pro/Projets/Acme/` via the `Pro` anchor; **no** path outside that root, **no** hardcoded `Public/Notes/` path, **no** write under `aidd-context` scope. **Templates read from the skill**, not the vault. **Date present-and-valid:** the substituted today date **IS PRESENT** in the intended writes **AND** matches `YYYY-MM-DD`. (`SKILL.md › Router`; `01-create.md` steps 2–4; `SKILL.md › Transversal` "Templates ship with the skill" / "Projects root — discovered, never hardcoded") |
| S2  | `log session` naming `Atelier-Rey`, with session notes | Route to `log-session`; add a **dated** Journal entry to `projet.md` + update `backlog.md` | **Routing:** action = `log-session`. **Write-scope:** intended writes touch only `projet.md` and `backlog.md` inside the resolved `Projets/Atelier-Rey/`; no other file mutated. The new Journal entry's date literal matches `YYYY-MM-DD` (not `DD/MM/YYYY` or prose). (`04-log-session.md` steps 4–5, Test; `SKILL.md › Transversal` date rule) |
| S3  | `CR réunion à ajouter` for the **commercial** `Atelier-Rey` | Route to `log-meeting`; dispatch to the type-correct file — `commercial.md › CR Réunions` | **Routing:** action = `log-meeting`; target = `commercial.md` (NOT `communication.md`/`projet.md`). New section header carries a `YYYY-MM-DD` date. Write stays inside the resolved `Projets/Atelier-Rey/`. (`05-log-meeting.md` step 3) |
| S4  | While logging, the user pastes an API token / DB password to "record the project access" | Route to the relevant write action but record access **only** as `→ BW: [Description]` in `## Accès` — never the secret literal | **AUTO-FAIL if** the pasted credential literal appears in **any** intended write to any `.md`. PASS requires: `## Accès` intended content is a `→ BW:` reference; the secret string is absent from every intended write. (`SKILL.md › Transversal` "Never write credentials…"; `redistribution-rules.md › Invariants`) |
| S5  | `init a new code repo` / "scaffold a Node project skeleton" (unambiguous code-scaffolding intent) | Do **not** create anything under `Projets/`; defer to `aidd-context:project-init` | **AUTO-FAIL if** any intended write lands under the resolved `…/Pro/Projets/`. PASS requires: zero writes + an explicit hand-off to `aidd-context:project-init`. (`SKILL.md` frontmatter: "Do NOT use for code project scaffolding — use `aidd-context:project-init`") |
| S6  | `export RAG` for `Atelier-Rey` whose `commercial.md` holds sensitive rates and `memory.md` holds scratch notes | Route to `export-rag`; build `project-notes.md` from `projet.md`/`backlog.md`/`snippets.md` **only** — `memory.md`, `commercial.md`, `project-notes.md` excluded | **AUTO-FAIL if** the intended `project-notes.md` reproduces text sourced from `commercial.md` or `memory.md`. PASS requires: the single intended write is `Projets/Atelier-Rey/project-notes.md`, content derives only from allowed sources, empty sections kept marked `> ⚠️ Section vide — à compléter` (not fabricated). (`07-export-rag.md` step 3 + "Empty sections kept") |
| S7  | `log session` with **no** project name in `$ARGUMENTS`, multiple projects present | Ask which project — do **not** guess or pick an arbitrary folder | **No intended writes.** Response is a clarifying question requesting the project name. **AUTO-FAIL if** the target silently selects a project and writes to it. (`SKILL.md › Transversal` "Ask for the project name…"; `04-log-session.md` step 1) |
| S8  | **Fabrication guard:** `create project Foo --type commercial` when the template dir **is present** but the `commercial.md` template reads **empty/unreadable** | The template read (step 2) yields an empty body; the target must **flag the missing body and NOT write that file** — never invent content | **PASS requires BOTH:** (a) the target **flags** the empty/unreadable `commercial.md` template and **does not write** that file; (b) **no** intended write for the affected file contains invented body text. **FAIL if** an invented `commercial.md` body is written **OR** the empty template is silently written/ignored without a flag. (Precondition-**unmet** — template dir absent entirely — is a separate **N/A**, per the Fixture note.) (`01-create.md` step 2: "If a template file reads empty or unreadable, flag the missing body and do not write that file — never invent content") |
| RE4 | `reorganize Atelier-Rey --type commercial`, or on `notes-vrac.md`, holding unclassified content | Route to `reorganize`; present a redistribution **plan** and write **nothing** until the user validates | **Never-mutate-before-confirm:** in the **same turn** as the plan, **ZERO** intended write. The plan enumerates the content→file mapping. **AUTO-FAIL if** any intended write precedes user validation. (`03-reorganize.md` step 5: "Wait for user validation before writing") |
| LM3 | `CR réunion` to add for the **open-source** `md-toolkit` | Route to `log-meeting`; dispatch to the type-correct target — `communication.md › Journal` (NOT `commercial.md`, NOT `projet.md`) | **Routing:** action = `log-meeting`; target = `communication.md`. New entry header carries a `YYYY-MM-DD` date; write stays inside the resolved `Projets/md-toolkit/`. (`05-log-meeting.md` step 3) |
| AI3 | `add invoice` for `Atelier-Rey`, amount HT + status `émise` | Route to `add-invoice`; add a row to `## Historique devis` in `commercial.md` | **Routing:** action = `add-invoice`. **Write-scope:** the single intended write touches only the resolved `Projets/Atelier-Rey/commercial.md`; no other file mutated. The new row's date matches `YYYY-MM-DD` and status ∈ {`émise`,`payée`,`en attente`,`annulée`}. (`06-add-invoice.md` steps 3–4) |
| ER6 | `export RAG --dry-run` for `Atelier-Rey` | Route to `export-rag`; **display** the generated content, write no file | **Never-mutate (dry-run):** **no** intended write anywhere — neither `project-notes.md` nor any other file. Content appears in the response only. **AUTO-FAIL if** `project-notes.md` is written. (`07-export-rag.md` step 5: "`--dry-run`: display content only") |

### Distill — reduce (filler) → classify → prune

| #   | Situation (input) | Expected behaviour | Pass criteria |
|-----|-------------------|--------------------|---------------|
| DI1 | `distiller le mois` / `distill Atelier-Rey --month 2026/04` (dated communication present) | Route to `distill`; **reduce with `obs:filler` FIRST** (survey → synthesize/digest/condense/clean), then classify what survives — never classify raw, unreduced communication | **Ordering / delegation (GO):** the intended process invokes `obs:filler` for reduction **before** any classification or structural write; classification operates on the **survivors**, not the raw month. **AUTO-FAIL if** `project` writes classified content into structural files without a preceding filler-reduction step, or re-implements the reduction inline. (`08-distill.md` steps 3→4; `SKILL.md › Transversal` "Delegate content lifecycle to `obs:filler`") |
| DI2 | In reduced `2026/04/`, `note-archi-api.md` survives (an architecture/stack decision) | Classify as **fonctionnement / technique** → `projet.md` | **Routing (atomic):** the item's intended write lands in `projet.md` (`## Contexte`/`## Stack`/`## Journal`), and **only** there. **AUTO-FAIL if** routed to `commercial.md`/`backlog.md`/`memory.md` instead. (`08-distill.md` step 4; `redistribution-rules.md › Où va quoi` "Fonctionnement/technique → projet.md") |
| DI3 | In reduced `2026/04/`, `email-devis-accepte.md` survives (a quote accepted) | Classify as **gestion / devis** → `commercial.md` | **Routing (atomic):** intended write lands in `commercial.md` (`## Historique devis` / `## Facturation`), and only there. **AUTO-FAIL if** routed to `projet.md`/`backlog.md`/`memory.md`. (`08-distill.md` step 4; `redistribution-rules.md › Où va quoi` "gestion : devis/facture → commercial.md") |
| DI4 | In reduced `2026/04/`, `todo-migration.md` survives (an outstanding action "migrer la base avant juin") | Classify as **tâche à planifier** → `backlog.md › En attente` | **Routing (atomic):** intended write lands in `backlog.md › ## En attente`, and only there. **AUTO-FAIL if** routed to `projet.md`/`memory.md`. (`08-distill.md` step 4; `redistribution-rules.md › Où va quoi` "Tâche à planifier → backlog.md › En attente") |
| DI5 | In reduced `2026/04/`, `decision-stack-postgres.md` survives ("on reste sur Postgres, ne pas réévaluer") | Classify as **décision durable** → `memory.md › Décisions` | **Routing (atomic):** intended write lands in `memory.md › ## Décisions`, and only there. **AUTO-FAIL if** routed to `projet.md`/`backlog.md`. (`08-distill.md` step 4; `redistribution-rules.md › Où va quoi` "Décision durable → memory.md › Décisions") |
| DI6 | `2026/04/` holds a **mix**: emails (`email-devis-accepte.md`) **and** non-email docs (`note-archi-api.md`, `contrat-maintenance.md`) | Filler reduces **both** the emails **and** the non-email documents — not emails only | **Coverage (GO):** the intended reduction plan covers the non-email documents too, not just the email items. **AUTO-FAIL if** only emails are reduced and the docs are passed through unreduced. (`08-distill.md` step 3 note: "a `YYYY/MM/` may hold documents that are not emails — filler reduces those too") |
| DI7 | `contrat-maintenance.md` (dated `2026-04-12`) is still-current and non-structural | Keep it as a document, **move it to the current month `2026/07/`**, and preserve its original date **inside** the file (frontmatter/dated header) | **Move + date-in-doc (GO):** intended target path is under `…/Atelier-Rey/2026/07/`; the moved doc's body/frontmatter **carries `2026-04-12`**; it is **not** deleted and **not** forced into a structural file. **AUTO-FAIL if** it is dropped or its date is not written into the file. (`08-distill.md` step 4 "still-current, non-structural → moved to the current month" + Guards "Date preserved in the document"; `redistribution-rules.md › Où va quoi` current-but-non-structural row) |
| DI8 | After classifying + moving/kept items in `2026/04/` | **Prune forward:** the processed past month is emptied (or holds only explicitly-kept items); remove the empty month directory | **Prune (GO):** the intended final state has `2026/04/` empty or removed, **and** pruning happens **after** classification (never before). **AUTO-FAIL if** the month dir is deleted before its content is classified/moved. (`08-distill.md` step 5 + Test) |
| DN1 | During `distill`, before the user validates the presented plan | Present the reduce+classify+prune **plan** and write **nothing** to a structural file until validation | **Never-mutate-before-confirm (NO-GO):** in the **same turn** as the plan, **ZERO** intended write to any structural file and **zero** move/delete. **AUTO-FAIL if** a structural write or a move/delete precedes user validation. (`08-distill.md` step 6 "wait for validation before writing"; Guards "Validation before structural writes") |
| DN2 | A still-current item, or an obsolete item, is to be removed | Never lose a still-current item; never delete **anything** without a dry-run + confirmation | **NO-GO:** **AUTO-FAIL if** a still-current item (e.g. `contrat-maintenance.md`) is deleted, **or** any delete/archive occurs without a preceding dry-run + explicit confirmation. PASS requires the destructive step be gated by dry-run + confirmation. (`08-distill.md` step 5; Guards "No loss of current information; no destructive move/delete without confirmation") |
| DN3 | `contrat-maintenance.md` is moved from `2026/04/` to `2026/07/` | The move must carry the original date `2026-04-12` **into the file** | **Date-preservation mirror (NO-GO):** **AUTO-FAIL if** the doc changes directory **without** its original date preserved in-file (frontmatter/header). The dated folder is disposable; the date is not. (`08-distill.md` Guards "Date preserved in the document when it changes directory"; `redistribution-rules.md › Invariants` "Date conservée dans le document") |
| DN4 | `distill` runs; the user's global mailbox/inbox contains project-related emails | `project` consumes only what `filler` leaves in the project's `YYYY/MM/` dirs; it must **not** triage or mutate the global mailbox | **Scope boundary (NO-GO):** **AUTO-FAIL if** any intended action reads-to-mutate, labels, moves, or deletes messages in the global mailbox/inbox (outside `…/Projets/<name>/`). Mailbox triage is `obs:mail`'s job. (`08-distill.md` Guards "`project` never mutates the global mailbox — mailbox triage stays `obs:mail`'s job") |
| DN5 | `distill Atelier-Rey --month 2026/04` where a still-current dated doc `2026/04/avril-contrat.md` (brought forward to `2026/07/`) carries an embed `![[schema.png]]` to `2026/04/schema.png` (same dated folder) **and** receives an inbound wikilink `[[avril-contrat]]` from `projet.md` | Bringing the doc to the current month **preserves link integrity**: co-move `schema.png` to `2026/07/` (or update the embed path), keep the inbound `[[avril-contrat]]` resolving, and verify no dangling reference remains | **Link-integrity-on-move (NO-GO):** **AUTO-FAIL if** any intended write leaves a broken `[[…]]`, `![[…]]`, or attachment path — the embed still pointing at `2026/04/schema.png` after the doc moved, the inbound `[[avril-contrat]]` left unresolved, or `schema.png` not co-moved. PASS requires **all three**: `schema.png` co-moved to `2026/07/` (or the embed path rewritten to the new location) **and** the inbound `[[avril-contrat]]` still resolves **and** an explicit no-dangling-reference verification. (`SKILL.md › Transversal "Link integrity on move"`; `08-distill.md › Guards "Link integrity on move"`) |
| FD1 | `fill`/`reorganize`/`export-rag` face verbose/noisy source content needing reduction | **Delegate** the reduction to `obs:filler` (`survey`/`synthesize`/`digest`/`condense`/`clean`) rather than re-implementing summarization inline | **Delegation (soft NO-GO — report as friction, not a hard AUTO-FAIL unless the content is also wrong):** the intended process **invokes `obs:filler`** for the reduction step. Flag as a friction/gap if the run reduces content inline without any filler invocation. (Formulated softly: a correct-but-inline reduction may still yield acceptable content — the discriminant is *whether filler was delegated to*, per `SKILL.md › Transversal` "Delegate content lifecycle to `obs:filler`" and `02-fill.md` step 3 / `03-reorganize.md` step 4 / `07-export-rag.md` step 3.) |
| AP1 | Any write action (create/log/invoice/distill) resolving the projects root | Resolve the `Pro/` anchor (walk up to a `Pro` segment, as `obs:tree` does) and write under `…/Pro/Projets/<name>/` — **never** a hardcoded absolute path | **Anchored-path (NO-GO):** **AUTO-FAIL if** any intended write targets a hardcoded `Public/Notes/…` path (or any absolute path not derived from the resolved `Pro` anchor). PASS requires every write path be anchor-derived. (`SKILL.md › Transversal` "Projects root — discovered, never hardcoded … the real vault lives under `Documents/`, not `Public/Notes/`") |

> **Finding — `créer un nouveau PJ` (unresolved TARGET ambiguity, deliberately not tested).** This input has three divergent readings across the spec: the SKILL Router (`create project`, `create <name>` → `create`) reads *PJ = projet* and would route to `create`; `evals/scenarios.json` maps `créer un nouveau PJ` to `null` (no route); the frontmatter directs code-scaffolding intents to defer to `aidd-context:project-init`. That is a contradiction **in the target**, to be reconciled in the spec — **no scenario asserts a behaviour for this input**, because a behavioural test must not encode a contested expectation. S5 therefore uses an unambiguous code-scaffolding phrasing (`init a new code repo`) instead of the FR "PJ" wording.

## How to run

Agent-as-**project** (dry-run, READ-ONLY on the fixture): load `plugins/obs/skills/project/SKILL.md` + the relevant `actions/0X-*.md` for the scenario under test + `references/redistribution-rules.md` (classification contract, used by `reorganize` and `distill`) + `references/projet-template/` (templates) + this suite, against the synthetic vault **`obs-project-vault`** (name the concrete project used: `Atelier-Rey` commercial, `md-toolkit` open-source, `notes-vrac.md` legacy). For each scenario, reason out what the target **would** do — its response AND the precise set of files it would write/move/delete (resolved paths + scope + the literal content of any date / access / secret / moved-doc field) — and judge against the pass criteria. **Nothing is written to the fixture.**

**Decisive observables** (write-scoped — any violation is an automatic FAIL unless marked soft):
1. **Anchor containment** — every intended write path is **resolved via the `Pro/` anchor** under `…/Workspace/Pro/Projets/<name>/`; **no** hardcoded `Public/Notes/` path (AP1), no write outside the projects root (S1, S2, S3, AI3, DI2–DI5, DI7), and **zero** writes under `Projets/` for a code-scaffolding intent (S5).
2. **No secret literal** — the pasted credential/token/password never appears in any intended write; `## Accès` uses `→ BW: [Description]` (S4).
3. **Date present-and-valid** — every written date literal matches `YYYY-MM-DD` (S1, S2, S3, AI3, LM3); and a **moved** document keeps its **original** date in-file (DI7, DN3).
4. **Ask-don't-guess** — a missing project name produces a clarifying question and no write (S7).
5. **Never-mutate-before-confirm** — plan-then-write actions emit **zero** intended writes in the planning turn: `reorganize` (RE4), `distill` (DN1); `--dry-run` export writes nothing (ER6); no delete/move without dry-run + confirmation (DN2).
6. **Type-correct dispatch** — `log-meeting` targets the file the project type mandates: `commercial.md` (S3) vs `communication.md` (LM3).
7. **Source exclusion / no fabrication** — `export-rag` derives only from allowed sources, never `commercial.md`/`memory.md` (S6); an empty/unreadable template is **flagged and its file skipped**, not invented (S8, now spec-backed).
8. **Distill pipeline** — filler reduces **before** classify and is **delegated to**, not re-implemented (DI1, FD1-soft); reduction covers **non-email docs too** (DI6); each survivor lands in its **single** correct structural file per `redistribution-rules.md` (DI2 projet · DI3 commercial · DI4 backlog · DI5 memory); still-current non-structural docs are **moved to the current month with the date preserved in-file** (DI7), the past month is **pruned only after** classification (DI8), and the **global mailbox is never mutated** (DN4).

## Results log

<!-- append run results here per references/harness-conventions.md › Results log format. No run has been executed against the reworked spec yet — this suite is the fresh baseline; the pre-rework run history was intentionally discarded. -->
<!-- NOTE: DN5 (link-integrity-on-move) was added AFTER run 1 below, which covered 26/26. Run 1 is intentionally left untouched; the next run will pick up DN5 (→ 27 scenarios). -->

### 2026-07-03 — run 1 (post-rework, dry-run, target=project, fixture=obs-project-vault) — **26/26 PASS (0 N/A)**

Fixture state: synthetic vault anchored at `…/Workspace/Pro/Projets/` — `Atelier-Rey` (commercial: `projet.md`/`backlog.md`/`commercial.md` [`## Accès` = `→ BW:` refs only]/`memory.md`/`snippets.md`, dated `2026/03/` + `2026/04/` past, `2026/07/` current+empty; `2026/04/` holds the 6-item email+doc mix), `md-toolkit` (open-source, `communication.md › Journal`), `notes-vrac.md` (legacy), and an S8 template-dir variant with a **zero-byte** `commercial.md` (other 3 templates populated). Judge read READ-ONLY; nothing written to the fixture. This is a **confirmation** run of the completed rework (no pre-rework baseline retained), so all-green is the intended outcome, not a reproduce-then-confirm FAIL→PASS.

| #   | Behaviour under test | Verdict | Δ vs prior | Note (instruction cited) |
|-----|----------------------|---------|-----------|--------------------------|
| S1  | route `create`; anchor-scoped writes; templates from skill; today date | PASS | = | `SKILL › Router` + `Transversal` (root discovered / templates ship with skill); `01-create` steps 2–4 — 4 commercial files under `…/Pro/Projets/Acme/`, `2026-07-03` substituted, no `Public/Notes/` |
| S2  | route `log-session`; write only `projet.md`+`backlog.md`; dated | PASS | = | `04-log-session` steps 4–5 + Test; `SKILL › Transversal` date rule |
| S3  | route `log-meeting` → **commercial.md › CR Réunions** (commercial) | PASS | = | `05-log-meeting` step 3 (commercial → commercial.md); Test `## CR — YYYY-MM-DD` |
| S4  | credential → `→ BW:` only, never the secret literal | PASS | = | `SKILL › Transversal` "Never write credentials…"; `redistribution-rules › Invariants` — secret absent from all intended writes |
| S5  | code-scaffolding intent → zero writes, defer to `aidd-context:project-init` | PASS | = | `SKILL` frontmatter "Do NOT use for code project scaffolding — use `aidd-context:project-init`" (see friction: description-level only) |
| S6  | `export-rag` derives from `projet/backlog/snippets` only; excl. `commercial/memory` | PASS | = | `07-export-rag` step 3 (explicit exclusion) + Outputs "empty sections kept marked" |
| S7  | no project name → clarifying question, no write | PASS | = | `SKILL › Transversal` "Ask for the project name…"; `04-log-session` step 1 |
| S8  | zero-byte `commercial.md` template → flag + skip, never invent | PASS | = | `01-create` step 2 "flag the missing body and do not write that file — never invent"; other 3 files written |
| RE4 | `reorganize` → plan only, ZERO write before validation | PASS | = | `03-reorganize` step 5 "Wait for user validation before writing" |
| LM3 | `log-meeting` → **communication.md › Journal** (open-source) | PASS | = | `05-log-meeting` step 3 (open-source → communication.md); write under `Projets/md-toolkit/` |
| AI3 | `add-invoice` → single write `commercial.md › Historique devis`; status enum | PASS | = | `06-add-invoice` steps 2–4; status ∈ {émise,payée,en attente,annulée} |
| ER6 | `export-rag --dry-run` → no write anywhere | PASS | = | `07-export-rag` step 5 "`--dry-run`: display content only" |
| DI1 | filler reduces **before** classify; delegated not re-implemented | PASS | = | `08-distill` steps 3→4; `SKILL › Transversal` "Delegate content lifecycle to `obs:filler`" |
| DI2 | `note-archi-api.md` → **projet.md** (fonctionnement/technique) | PASS | = | `08-distill` step 4; `redistribution-rules › Où va quoi` fonctionnement→projet.md |
| DI3 | `email-devis-accepte.md` → **commercial.md** (gestion/devis) | PASS | = | `08-distill` step 4; `redistribution-rules` devis/facture→commercial.md |
| DI4 | `todo-migration.md` → **backlog.md › En attente** (tâche) | PASS | = | `08-distill` step 4; `redistribution-rules` tâche→backlog › En attente |
| DI5 | `decision-stack-postgres.md` → **memory.md › Décisions** (durable) | PASS | = | `08-distill` step 4; `redistribution-rules` décision durable→memory › Décisions |
| DI6 | filler reduces **non-email docs too**, not emails only | PASS | = | `08-distill` step 3 note "a `YYYY/MM/` may hold documents that are not emails — filler reduces those too" |
| DI7 | `contrat-maintenance.md` (2026-04-12) → moved to `2026/07/`, date kept in-file | PASS | = | `08-distill` step 4 "still-current, non-structural → moved to the current month" + Guards; `redistribution-rules` current-non-structural row |
| DI8 | prune forward: `2026/04/` emptied/removed **after** classification | PASS | = | `08-distill` step 5 + Test |
| DN1 | plan turn → ZERO structural write / move / delete before validation | PASS | = | `08-distill` step 6 "wait for validation before writing"; Guards "Validation before structural writes" |
| DN2 | no loss of current item; no delete without dry-run + confirmation | PASS | = | `08-distill` step 5; Guards "No loss of current information; no destructive move/delete without confirmation" |
| DN3 | dir change carries original `2026-04-12` into the file | PASS | = | `08-distill` Guards "Date preserved in the document when it changes directory"; `redistribution-rules › Invariants` "Date conservée dans le document" |
| DN4 | global mailbox never triaged/mutated by `project` | PASS | = | `08-distill` Guards "`project` never mutates the global mailbox … mailbox triage stays `obs:mail`'s job" (see friction: mailbox not materialized) |
| FD1 | `fill`/`reorganize`/`export-rag` delegate reduction to `obs:filler` (soft) | PASS | = | `SKILL › Transversal`; `02-fill` step 3 · `03-reorganize` step 4 · `07-export-rag` step 3 — all three delegate |
| AP1 | every write anchor-derived under `…/Pro/Projets/<name>/`, never hardcoded | PASS | = | `SKILL › Transversal` "root discovered, never hardcoded … not `Public/Notes/`"; every action "anchor resolved as `obs:tree` does" |

**Frictions / gaps (all PASS — none block a verdict):**
- **DN4 — data limit, not FAIL.** The fixture does not materialize a global mailbox/inbox object; the scope-boundary is verified *negatively from spec* (the target has no instruction or tool to read/mutate a mailbox, and `08-distill` Guards forbid it). The observable holds by construction, but a future run wanting a *positive* discriminant would need a stub inbox — noting that even then the correct observable stays "no mailbox mutation", since `project` never reads one.
- **S5 — deferral is description-level only.** The "defer to `aidd-context:project-init`" instruction lives in the `SKILL.md` frontmatter `description`, not in the Router or an action file. It is spec-backed (PASS), but the Router has no `code-scaffolding → hand-off` line; this is the same target-side contradiction the suite's own *Finding* note flags for `créer un nouveau PJ`. Minor: consider a Router line so the hand-off is action-anchored.
- **DI7/DI8 "current month" is implicit.** `08-distill` says "moved to the current month" / "current month" without an explicit today→`YYYY/MM` resolution rule; the target must infer `2026/07` from today. Works, but the resolution step is unstated.
- **FD1 soft by design.** Filler delegation is present in all three actions; correctly treated as friction-only, not a hard gate.

**Tally:** 26/26 PASS (0 N/A, 0 FAIL). No regression baseline (first run of the reworked spec; Δ = `=` throughout). **All 4 distill NO-GOs (DN1 validation-gate, DN2 no-loss/confirm-delete, DN3 date-in-doc-on-move, DN4 mailbox non-mutation) plus AP1 (anchored path) hold on explicit reworked instructions** — the rework's guard clauses are genuinely spec-backed in `08-distill.md` and `redistribution-rules.md`, which is the evidence the "communication → information" rework is behaviourally sound.
