{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "sc-css",
  "version": "0.1.0",
  "description": "CSS ecosystem skills: sniff (architecture + stack), audit (spécificité, code mort, magic numbers, a11y), improve (custom properties, cascade layers, modernisation), legacy (float→flex/grid, px→rem, préfixes→standard), teach. design-bridge: réceptacle du pivot design — tokens.json → custom properties CSS, components.json → BEM stylesheets.",
  "author": {
    "name": "François-Xavier Guillois"
  }
}
