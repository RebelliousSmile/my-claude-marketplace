# Example: Code Review Output

## Input (Git Diff)

```diff
diff --git a/src/services/userService.ts b/src/services/userService.ts
index abc1234..def5678 100644
--- a/src/services/userService.ts
+++ b/src/services/userService.ts
@@ -15,6 +15,20 @@ export class UserService {
+  async getUserById(id: string) {
+    const query = `SELECT * FROM users WHERE id = '${id}'`;
+    const result = await this.db.query(query);
+    return result[0];
+  }
+
+  async updateUser(id: string, data: any) {
+    try {
+      await this.db.update('users', data, { id });
+    } catch (e) {
+      // Log error
+    }
+    return true;
+  }
```

## Generated Review

```markdown
# Code Review Report

**Branch/PR:** feature/user-management
**Reviewed files:** 1
**Date:** 2026-01-21
**Review Type:** Quick (Skill)

---

## Summary

Changes add user retrieval and update functionality. Found critical SQL injection vulnerability and error handling issues that must be addressed before merge.

---

## Files Reviewed

- `src/services/userService.ts` - Modified (2 new functions)

---

## Findings

### Critical Issues

1. **[CRITICAL]** `src/services/userService.ts:17` - SQL Injection Vulnerability

**Issue:** User ID is directly interpolated into SQL query string, allowing SQL injection attacks.

**Fix:** Use parameterized queries

```typescript
// Before
const query = `SELECT * FROM users WHERE id = '${id}'`;
const result = await this.db.query(query);

// After
const query = 'SELECT * FROM users WHERE id = ?';
const result = await this.db.query(query, [id]);
```

---

### Warnings

1. **[WARNING]** `src/services/userService.ts:25` - Silent Error Handling

**Issue:** Catch block swallows error without proper handling. Function returns `true` even on failure.

**Fix:** Propagate error or return meaningful result

```typescript
// Before
try {
  await this.db.update('users', data, { id });
} catch (e) {
  // Log error
}
return true;

// After
try {
  await this.db.update('users', data, { id });
  return { success: true };
} catch (e) {
  logger.error('Failed to update user', { id, error: e });
  return { success: false, error: 'Update failed' };
}
```

---

2. **[WARNING]** `src/services/userService.ts:22` - Untyped Parameter

**Issue:** `data` parameter is typed as `any`, losing type safety.

**Fix:** Define proper interface

```typescript
interface UpdateUserData {
  name?: string;
  email?: string;
}

async updateUser(id: string, data: UpdateUserData) {
```

---

### Suggestions

1. **[SUGGESTION]** `src/services/userService.ts:16` - Add Input Validation

Consider adding UUID validation for the `id` parameter.

---

### Positive Points

- Clear function naming
- Async/await used correctly
- Functions are focused on single operations

---

## Checklist Results

| Category | Status |
|----------|--------|
| Functionality | OK |
| Code Quality | Warning |
| Security | Critical |
| Performance | OK |
| Testing | Not checked |
| Project Patterns | OK |

---

## Decision

- [ ] Approve
- [x] Request changes
- [ ] Comment only

---

## Next Steps

1. Fix SQL injection vulnerability (Critical)
2. Improve error handling in updateUser
3. Add proper typing for data parameter
4. Consider adding input validation

---

*Generated by /code-review*
```
